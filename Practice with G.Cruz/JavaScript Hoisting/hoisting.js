// hoisting
// =when JS declares variables/fucntions at the top of their scope regardless of where they're at 

// x = 1; does not work because it has not been declared.

// console.log(z);  reference error - has not been declared.

// Always declare varaibles at the top

let x = 0;
const y = 1;
 var z = 2;

console.log(z);
